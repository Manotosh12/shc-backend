import { SoilReportDistrictwise } from './soil-report-districtwise.entity';

describe('SoilReportDistrictwise Entity', () => {
  it('should be defined', () => {
    const entity = new SoilReportDistrictwise();
    expect(entity).toBeDefined();
  });

  // Add more tests as needed for entity logic, if any
});
